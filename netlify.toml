# Netlify 配置文件

[build]
  # 发布目录
  publish = "public"
  # Functions 目录
  functions = "netlify/functions"
  # 不需要构建命令，直接发布静态文件
  command = "echo 'No build needed'"

[functions]
  node_bundler = "esbuild"

# 重定向规则 - SPA 支持
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
